name: vigilant_spork
description: Vigilant spork project.
publish_to: 'none'
version: 0.2.0+alpha

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  cached_network_image: ^3.2.3
  device_info_plus: ^8.1.0
  firebase_analytics: ^10.1.4
  firebase_core: ^2.7.0
  firebase_remote_config: ^3.0.13
  flutter:
    sdk: flutter
  flutter_secure_storage: ^8.0.0
  http: ^0.13.5
  internet_connection_checker: ^1.0.0+1
  intl: ^0.18.0
  url_launcher: ^6.1.10
  webview_flutter: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
